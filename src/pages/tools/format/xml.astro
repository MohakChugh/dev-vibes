---
import ToolLayout from '@/layouts/ToolLayout.astro';
import { XmlFormatter } from '@/components/tools/format/FormatTools';
import { tools } from '@/config/tools';
const tool = tools.find(t => t.id === '24')!;
---
<ToolLayout {...tool}><XmlFormatter client:load /></ToolLayout>
