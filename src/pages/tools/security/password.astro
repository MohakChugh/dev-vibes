---
import ToolLayout from '@/layouts/ToolLayout.astro';
import { PasswordTool } from '@/components/tools/utility/UtilityTools';
import { tools } from '@/config/tools';
const tool = tools.find(t => t.id === '34')!;
---
<ToolLayout {...tool}><PasswordTool client:load /></ToolLayout>
